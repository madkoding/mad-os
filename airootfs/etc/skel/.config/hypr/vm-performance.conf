# Hyprland VM performance optimizations (auto-populated when running in a VM)
# This file is intentionally empty on bare metal hardware.
# When running inside VirtualBox/VMware/QEMU, session scripts populate it
# with optimizations to reduce mouse lag and improve responsiveness.

# madOS - Ensure iwd starts after persistence overlays are mounted
# This ensures iwd picks up any configuration changes from the persistent /etc
[Unit]
ConditionPathExists=/run/archiso
After=mados-persistence.service

[Install]
WantedBy=multi-user.target

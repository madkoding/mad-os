# Live ISO SSH configuration
# Password auth enabled (no way to pre-configure SSH keys on live ISO)
# Root login disabled (use mados user with sudo instead)
PasswordAuthentication yes
PermitRootLogin no
